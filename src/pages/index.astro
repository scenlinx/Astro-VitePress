---
import HomeLayout from '../layouts/HomeLayout.astro';
import { loadAllDocsMeta, buildNavItems } from '../config/docs';
import { siteConfig } from '../config/site';

const { allMeta } = await loadAllDocsMeta();
const navItems = buildNavItems(allMeta);
const homeEntry = allMeta.find(m => m.slug === 'index');
const homeFm = homeEntry?.frontmatter || {};
---

<HomeLayout 
  title={homeFm.title || siteConfig.name}
  description={homeFm.heroDesc || homeFm.description}
  keywords={homeFm.keywords}
  navItems={navItems}
>
  <div class="home">
    <div class="hero">
      <div class="logo">
        <img src={siteConfig.heroLogo} alt="Logo" width="120" height="120" fetchpriority="high" />
      </div>
      <h1>{homeFm.heroTitle}</h1>
      <p>{homeFm.heroDesc}</p>
      <div class="actions">
        <a href={homeFm.primaryActionLink || siteConfig.defaultHeroLink} class="btn primary">{homeFm.primaryAction}</a>
        <a href={homeFm.secondaryActionLink || '#'} target="_blank" class="btn">{homeFm.secondaryAction}</a>
      </div>
    </div>
    
    {homeFm.features && (
      <div class="features">
        {homeFm.features.map((f: { title: string; desc: string }) => (
          <div class="feature">
            <h3>{f.title}</h3>
            <p>{f.desc}</p>
          </div>
        ))}
      </div>
    )}
  </div>
</HomeLayout>
